{
  "name": "tiktok-downloader-extension",
  "version": "1.0.0",
  "description": "Chrome Extension to download TikTok videos easily",
  "author": "Người lạ ơi",
  "license": "MIT",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "clean": "rm -rf dist",
    "dev": "npm run clean && npm run watch",
    "release": "npm run clean && npm run build && echo 'Package dist/ into .zip for submission'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  },
  "keywords": []
}
